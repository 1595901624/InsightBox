export class DrawerLayoutController {
  /**
   * 打开抽屉
   * @param animate 是否使用动画，默认true
   */
  openDrawer: (animate?: boolean) => void = () => {

  }
  /**
   * 关闭抽屉
   * @param animate 是否使用动画，默认true
   */
  closeDrawer: (animate?: boolean) => void = () => {

  }
}